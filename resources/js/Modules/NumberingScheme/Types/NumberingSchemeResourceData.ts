export type NumberingSchemeResourceData = {
    id: number;
    prefix: string;
    name: string;
    folder_name: string;
    next_number: number;
    reset_frequency: string;
    add_if_approved: boolean;
};
